---
layout: wxBase
---
      <script>
        $(function(){
          var w=$(window).width();
          var s=w/1366;                             
          $('#menu').css('width',w);
          $('#name').css('margin-left',190*s);
          $('#name').css('font-size',26*s);
          $('nav').css('margin-left',130*s);
          $('nav').css('font-size',18*s);
          $('ul li').css('margin-left',30*s);
          $('#fan').css('margin-left',60*s);
          $('#foot').css('width',w);          
        })
      </script>
      <video id="viA" controls="controls" autoplay="autoplay">
        <source src="{{ site.url }}/video/wenXuan.webm" type="video/webm" />
        <source src="{{ site.url }}/video/wenXuan.mp4" type="video/mp4" />
        您的浏览器不支持。   
      </video>

   <script>
     $(function(){
       var w=915*[$(window).width()]/1353,h=w*9/16;
       $('video').css('width',w);
       $('video').css('height',h);
       var top1=([$(window).height()-119]-h)/2;
       $('video').css('margin-top',top1);
       $('section').css('height',($(window).height()-119));
     })
   </script>

